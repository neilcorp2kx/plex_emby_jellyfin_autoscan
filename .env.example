# =====================================================
# Plex/Emby/Jellyfin Autoscan Environment Configuration
# =====================================================
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!
#
# NOTES:
# - Do NOT use quotes around values
# - Leave variables blank or comment them out if not needed
# - Generate secure keys using the commands provided below
# - Boolean values should be: true or false (lowercase)
# =====================================================

# -----------------------------------------------------
# PLEX CONFIGURATION
# -----------------------------------------------------
# Plex Media Server authentication token
# Get your token: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
# Or use: python scripts/plex_token.sh
PLEX_TOKEN=

# Plex Media Server URL (local network address)
# Default: http://localhost:32400
# Docker users: use http://plex:32400 if Plex container is named 'plex'
PLEX_LOCAL_URL=http://localhost:32400

# -----------------------------------------------------
# JELLYFIN/EMBY CONFIGURATION
# -----------------------------------------------------
# Jellyfin or Emby API Key
# Jellyfin: Dashboard -> API Keys -> Add new key
# Emby: Settings -> Advanced -> API Keys -> New API Key
JELLYFIN_API_KEY=

# Media server type: jellyfin or emby (lowercase only)
# Default: jellyfin
EMBY_OR_JELLYFIN=jellyfin

# -----------------------------------------------------
# SERVER SECURITY (REQUIRED)
# -----------------------------------------------------
# Flask secret key for session management
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Use a strong random value and keep it secret!
SECRET_KEY=

# Server webhook password (used in webhook URL)
# Generate: python3 -c "import uuid; print(uuid.uuid4().hex)"
# This will be part of your webhook URL: http://your-server:3467/YOUR_SERVER_PASS
SERVER_PASS=

# Optional: Old secret keys for graceful rotation (comma-separated, no spaces)
# When rotating keys: Generate new SECRET_KEY, move old key here
# This allows existing sessions to remain valid during key rotation
# SECRET_KEY_FALLBACKS=old_key_1,old_key_2

# -----------------------------------------------------
# SECURITY HEADERS (OPTIONAL - Production with HTTPS)
# -----------------------------------------------------
# Enable Flask-Talisman security headers (HSTS, X-Frame-Options, etc.)
# IMPORTANT: Only enable when using HTTPS to avoid browser warnings
# Default: false
# ENABLE_TALISMAN=false

# Force HTTPS redirects (only use with HTTPS enabled)
# Default: false
# FORCE_HTTPS=false

# Secure session cookies (only send over HTTPS)
# IMPORTANT: Only enable when using HTTPS
# Default: false (set to true in production with HTTPS)
# SESSION_COOKIE_SECURE=false

# -----------------------------------------------------
# SERVER CONFIGURATION (OPTIONAL OVERRIDES)
# -----------------------------------------------------
# Server IP address to bind to
# Default: 0.0.0.0 (all interfaces)
# Docker users: Keep as 0.0.0.0
# SERVER_IP=0.0.0.0

# Server port
# Default: 3467
# Docker users: Map this port in your docker-compose.yml
# SERVER_PORT=3467

# -----------------------------------------------------
# PLEX USER CONFIGURATION (OPTIONAL OVERRIDES)
# -----------------------------------------------------
# Plex service user (for running Plex Media Scanner)
# Default: plex
# PLEX_USER=plex

# -----------------------------------------------------
# DOCKER CONFIGURATION (OPTIONAL)
# -----------------------------------------------------
# Enable Docker mode (runs Plex commands inside Docker container)
# Default: False
# USE_DOCKER=False

# Docker container name (if USE_DOCKER=True)
# Default: plex
# DOCKER_NAME=plex

# -----------------------------------------------------
# SUDO CONFIGURATION (OPTIONAL)
# -----------------------------------------------------
# Use sudo when running Plex Media Scanner
# Default: True (set to False if running as plex user or in Docker)
# USE_SUDO=True

# =====================================================
# GENERATION COMMANDS
# =====================================================
# Generate SECRET_KEY:
#   python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
#
# Generate SERVER_PASS:
#   python3 -c "import uuid; print('SERVER_PASS=' + uuid.uuid4().hex)"
#
# Get Plex Token:
#   python scripts/plex_token.sh
#   OR visit: https://support.plex.tv/articles/204059436/
#
# =====================================================
# ADDITIONAL CONFIGURATION
# =====================================================
# All other configuration options (paths, mappings, scan priorities, etc.)
# should be set in config/config.json
# See config.example.json for a complete example
# =====================================================
